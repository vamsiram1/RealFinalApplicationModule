import React from 'react';
import DamagedRefactored from '../../../Damaged/DamagedRefactored';

const DamagedStatusForm = ({
  initialValues,
  onSubmit
}) => {
  return (
    <DamagedRefactored />
  );
};

export default DamagedStatusForm;
